spring:
  # Application Name
  application:
    name: WishListService

  # Currently Activated Profile
  profiles:
    active: dev

  # Devtools Auto Reload
  devtools:
    restart:
      enabled: true